import React from 'react';

import TextField from './TextField';

export default {
  title: `atoms|TextField`,
  component: TextField,
};

export const TextField_ = () => {
  return <TextField />;
};
